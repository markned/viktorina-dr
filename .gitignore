node_modules
dist
*.local
.DS_Store
*.tsbuildinfo

# Локальные черновики редактора (экспорты, загрузки) — не коммитить
editor-local/

# Локальные git bundle / архивы перед крупными изменениями
_backup/

# Случайный emit tsc рядом с конфигом Vite (используем только .ts)
vite.config.js
vite/**/*.js
vite/**/*.d.ts

# Исходник >100MB (ролик для dock-chroma); в репозитории — только dock-chroma.mp4
public/content/video/ui/1080P.mov
