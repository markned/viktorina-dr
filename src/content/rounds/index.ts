/**
 * Данные раундов викторины (`rounds.ts`).
 * Доп. набор для сборки/редактора: `addedTracks2025.ts` (генерация: `npm run build:added-tracks`).
 */
export { rounds } from "./rounds";
export { addedTracks2025 } from "./addedTracks2025";
export type { Round, RoundBackgroundYoutube } from "../../types";
