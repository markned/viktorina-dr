import { DEFAULT_QUIZ_SESSION_LENGTH } from "../helpers/quizConfig";

/** Правила игры для гостей (до 2000 символов) */
export const GAME_RULES = `Ваша задача: продолжить текст Паши Техника из группы Kunteynir — тот, чей вариант ближе всех к оригиналу, получает баллы за раунд.
Креативные и угарные ответы, даже если они неправильные также вознаграждаются баллами.

Сначала звучит отрывок песни, на экране появляются строки, которые вы слышите. 

Когда фрагмент заканчивается, даётся ограниченное время на ответ — до сигнала таймера. 
После сигнала каждый озвучивает свой вариант, после чего ведущий открывает правильный ответ, песня продолжает играть, и мы переходим к следующему раунду.

В одной игре — ${DEFAULT_QUIZ_SESSION_LENGTH} раундов — удачи!`;

/** Правила режима «Викторина» (варианты ответа) */
export const GAME_RULES_QUIZ = `Режим «Викторина»: после отрывка трека нужно выбрать правильную строку из четырёх вариантов.

Пока идёт таймер, нажмите на вариант, затем «Подтвердить ответ» — или дождитесь конца времени: если вариант уже выбран, засчитается как подтверждение. Если время вышло и вы ничего не выбрали, показывается правильный ответ без балла.

Правильные ответы суммируются. В конце игры показывается итоговый ролик в зависимости от результата.

В одной игре — до ${DEFAULT_QUIZ_SESSION_LENGTH} раундов (только треки с одной строкой ответа). Удачи!`;